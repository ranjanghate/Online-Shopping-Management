<nav class="navbar navbar-light bg-light">
<%= link_to image_tag("logos/logo.png", width: '300px'), home_path %>
  <% if user_signed_in? %>
    <div class="nav-field">
      <div class="nav-item">
        <%= link_to "Home", home_path %>
      </div>
      <div class="nav-item">
        <%= link_to "Courses", courses_path %>
      </div>
      <%= "Logged in as" %>
      <div class="nav-item">
        <%= link_to "#{Current.user.email}", users_path(Current.user) %>
      </div>
      <div class="nav-item">
        <%= link_to "Logout", destroy_user_session_path, method: :delete %>
      </div>
    </div>
  <% else %>
    <div class="nav-field">
      <div class="nav-item">
        <%= link_to "Home", home_path %>
      </div>
      <div class="nav-item">
        <%= link_to "Courses", courses_path %>
      </div>
      <div class="nav-item">
        <%= link_to "Login", new_user_session_path %>
      </div>
      <div class="nav-item">
        <%= link_to "Create Account", new_user_registration_path %>
      </div>
    </div>
  <% end %>
</nav>
